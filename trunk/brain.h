/*
class brain{
public:
	brain(int min1, int min2, int min3, int min4){
	
	};

	int updateLevels()
	{
		for (int i = 0; i < NUM_NEEDS; i++){
			if (levels[i]< 100) levels[i]++;
		}
		
	};

	int updateGoals(){
		for (int i = 0; i < NUM_NEEDS; i++){
			if (levels[i]< minLevels[i])
					setGoal(i);
		}

	}

	int levels[NUM_NEEDS];
	int minLevels[NUM_NEEDS]

	std::vector goalStack;


}

*/
